services:
  # Prefer ./test.sh for faster cached builds
  # docker compose run test still works but doesn't benefit from BuildKit caching
  test:
    build:
      context: .
      dockerfile: Dockerfile
      target: test
